import Foundation

/* ================= Array ================

- var vs let

var values: [Int] = [2, 3, 4, 5, 6]
var values2: [String] = ["ahoJ", "jak", "se", "mas"]
values[1]

print(values.count)
values.append(10) //prida na konec pole
values.dropLast() //odebere posledni
 */

/*
 Training tasks

 1. Declare array. Select data type on your own. Add some elements to it. Use for loop to print out elements of array.
 var values: [Int] = [2, 4, 7, 10]

 2. Declare array and add 5 elements to it using for loop.

 3. Declare array of numbers. Fill it with some values. Now use for loop to add 2 to each element of array
 
 - Declare some array and add some elements to it. Write condition. If number of elements in array is lower then 3 then add one element to array otherwise print out number of elements in array.

 - Declare variable called `arr`. Type is Array containing numbers.
 - Add 3 elements to `arr`.
 - Print second element from `arr`.
 - Remove last element from `arr` and print it out.


 */

