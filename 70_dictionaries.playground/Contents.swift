import UIKit
import Foundation

// ================= Dictionary ================

// structure
// key/value
// unique keys

/*
 ---------- TASK: Words
 Write function that will take array of strings as parameter. Return N most occurring words. When input array is empty return nil (use guard here).

 Examples:
 words(input: ["hello", "apple", "apple", "showmax", "whatever", "showmax", "apple", "showmax", "apple"], amount: 2) -> ["apple", "showmax"]
 */


/*
 Tasks

 1. Declare variable called `dict`. Type is Dictionary where key is Strings and Elements are numbers.

 2. Store three values in `dict`.

 3. Print out one of the values from `dict`.

 4. Remove one of the values from `dict`.

 5. Print number of elements in `dict`.

 */
