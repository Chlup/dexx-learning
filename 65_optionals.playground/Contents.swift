import UIKit
import Foundation

// ================= Optionals ================

// nil value
// Any type can be optional.
// Optional unwrapping, force unwrapping

/*
 Tasks

 1. Declare optional variable and safely get value from it. If value is there print out value. Otherwise print error message.

 2. Write find a number function. That function will have one number and array of numbers as inputs. If number is in that array return index of that
    number in that array. Otherwise return nil.
 */
